/**
 * Values for `<input type="file" accept>`. Kept in sync with the server
 * whitelist in `uploads.ts` so the OS picker and the API agree.
 */
export const UPLOAD_ACCEPT =
  "image/jpeg,image/png,image/webp,image/avif,image/gif,.jpg,.jpeg,.png,.webp,.avif,.gif,application/pdf";

export const IMAGE_UPLOAD_ACCEPT =
  "image/jpeg,image/png,image/webp,image/avif,image/gif,.jpg,.jpeg,.png,.webp,.avif,.gif";
